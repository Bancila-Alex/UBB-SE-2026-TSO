<Window
    x:Class="ChatModule.views.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:ChatModule.views"
    mc:Ignorable="d"
    Title="Login"
    Height="400"
    Width="350"
    WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <local:LoginViewModel />
    </Window.DataContext>

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox
            Grid.Row="0"
            Height="32"
            Margin="0,0,0,4"
            PlaceholderText="Username"
            Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

        <PasswordBox
            Grid.Row="2"
            Height="32"
            Margin="0,0,0,4"
            x:Name="PasswordBox"
            PasswordChanged="PasswordBox_PasswordChanged"
            Tag="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <Button
            Grid.Row="4"
            Height="36"
            Content="Login"
            Command="{Binding LoginCommand}"
            IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"
            />

        <TextBlock Grid.Row="6" HorizontalAlignment="Left">
            <Hyperlink Command="{Binding ForgotPasswordCommand}">
                Forgot password?
            </Hyperlink>
        </TextBlock>

        <TextBlock Grid.Row="8" HorizontalAlignment="Left">
            <Hyperlink Command="{Binding GoToRegisterCommand}">
                Sign Up
            </Hyperlink>
        </TextBlock>

        <TextBlock
            Grid.Row="10"
            Foreground="Red"
            Text="{Binding ErrorMessage}"
            Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"
            TextWrapping="Wrap"
            />

        <Grid Grid.Row="11" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ProgressBar
                IsIndeterminate="True"
                Width="100"
                Height="4"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </Grid>
    </Grid>
</Window>